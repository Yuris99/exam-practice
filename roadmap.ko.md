# MVP 이후 로드맵

## 1. 현재 기준선

계정 없이 브라우저 로컬에서 사용하는 개인 학습용 MVP는 완료됐다. 정답 비공개는 요구하지 않는다. AI 버튼, 한국어 프롬프트, API 경로, 저장, 캐시 및 사용 제한은 선택적 연동 골격으로 구현되어 있으며 실제 유료 제공자 연결은 MVP 필수 조건이 아니다.

## 2. 바로 진행할 작업

1. 로컬 `dev` 브랜치를 푸시하고 통합 브랜치로 사용한다.
2. 미리보기 배포를 만들고 HTTPS 환경에서 PWA 설치를 확인한다.
3. 코드, 보기 영역, 여러 줄 선택지 및 이미지가 포함된 필기·실기 대표 문제 50–100개를 가져온다.
4. 아이폰 크기 기기, 안드로이드 크기 기기, 데스크톱 브라우저에서 연습과 시험 전체 흐름을 확인한다.
5. 더 큰 기능을 추가하기 전에 실제 콘텐츠 입력과 실제 기기 사용에서 발견된 문제만 우선 수정한다.

## 3. 콘텐츠 품질

- 문제, 선택지, 정답, 해설, 출처, 연도, 이미지, 코드 및 게시 상태를 확인하는 반복 가능한 검수 목록을 정의한다.
- 실제 대량 파일로 중복 감지와 CSV 오류 메시지를 검증한다.
- 정보처리기사 기본 또는 가져오기 문제 세트를 확장한다.
- 실제 문제에서 필요성이 확인될 때만 표, 수식 또는 더 풍부한 콘텐츠 블록을 추가한다.

## 4. 제품 품질

- 새로고침 복구, 시간 종료 제출, 오프라인 재실행, 백업 복원 및 과거 결과 고정에 대한 브라우저 수준 테스트를 추가한다.
- 자동 접근성 검사와 수동 키보드 이동 테스트를 수행한다.
- 실제 기기에서 안전 영역, 가상 키보드, 긴 코드, 여러 이미지 및 매우 긴 선택지를 확인한다.
- 테스트, TypeScript 및 프로덕션 빌드를 위한 CI 검사를 추가한다.

## 5. 선택적 AI 활성화

- AI 해설이 필요해질 때만 운영 API 키를 연결한다.
- 고정된 실제 문제 표본으로 한국어 해설 정확도를 평가한다.
- 응답 시간, 실패율, 이미지 문제 품질 및 해설당 비용을 측정한다.
- 제공자를 사용할 수 없을 때도 저장된 해설은 계속 표시한다.
- AI 기반 실기 채점은 선택 사항이며 참고용으로만 제공한다.

## 6. 배포 및 운영

- 호스팅 환경을 선택하고 서버 전용 환경 변수를 설정한다.
- 학습자 신원을 수집하지 않는 가벼운 오류 및 API 사용량 모니터링을 추가한다.
- 저장 구조를 변경하기 전에 백업과 복원 방법을 문서화한다.
- `dev` 브랜치에서 검증한 뒤 안정 버전에 태그를 추가한다.

## 7. 명시적 제외 범위

- 계정과 로그인
- 서버 측 정답 비공개 또는 부정행위 방지
- 결제, 순위, 댓글 또는 소셜 기능
- 네이티브 모바일 앱
- 필수적인 실제 AI 의존성
