# 컴시기 & 임베기 공부 정리

## 1. 컴시기 핵심 정리

> 컴시기 = 컴퓨터 시스템 기초/컴퓨터 시스템 관련 과목으로 보고 정리함.

### 1.1 컴퓨터 시스템 구성

컴퓨터 시스템은 크게 **하드웨어**, **소프트웨어**, **사용자**, **데이터**로 구성된다.

| 구성 요소 | 설명 |
|---|---|
| 하드웨어 | CPU, 메모리, 저장장치, 입출력장치 등 물리적 장치 |
| 소프트웨어 | 운영체제, 응용 프로그램, 시스템 프로그램 |
| 사용자 | 컴퓨터를 사용하는 사람 또는 다른 시스템 |
| 데이터 | 프로그램이 처리하는 입력값과 결과 |

### 1.2 CPU

CPU는 명령어를 해석하고 실행하는 컴퓨터의 핵심 장치다.

| 구성 요소 | 역할 |
|---|---|
| 제어장치 CU | 명령어 해석, 제어 신호 생성 |
| 산술논리장치 ALU | 산술 연산, 논리 연산 수행 |
| 레지스터 | CPU 내부의 고속 임시 저장 공간 |
| 캐시 | CPU와 메모리 속도 차이를 줄이는 고속 메모리 |

#### 명령어 실행 과정

1. **Fetch**: 메모리에서 명령어를 가져온다.
2. **Decode**: 명령어를 해석한다.
3. **Execute**: 연산 또는 데이터 이동을 수행한다.
4. **Write Back**: 결과를 레지스터나 메모리에 저장한다.

### 1.3 메모리 계층 구조

빠르고 비싼 저장장치는 CPU 가까이에, 느리고 저렴한 저장장치는 바깥쪽에 배치된다.

| 계층 | 속도 | 용량 | 특징 |
|---|---:|---:|---|
| 레지스터 | 가장 빠름 | 매우 작음 | CPU 내부 |
| 캐시 | 빠름 | 작음 | 자주 쓰는 데이터 저장 |
| 주기억장치 RAM | 보통 | 중간 | 실행 중인 프로그램 저장 |
| 보조기억장치 SSD/HDD | 느림 | 큼 | 영구 저장 |

#### 캐시 관련 개념

| 개념 | 설명 |
|---|---|
| 지역성 | 프로그램이 특정 데이터나 근처 데이터를 반복 접근하는 성질 |
| 시간 지역성 | 최근 사용한 데이터를 다시 사용할 가능성이 높음 |
| 공간 지역성 | 접근한 데이터 근처의 데이터를 사용할 가능성이 높음 |
| 캐시 히트 | 필요한 데이터가 캐시에 있음 |
| 캐시 미스 | 필요한 데이터가 캐시에 없어 메모리에서 가져와야 함 |

### 1.4 운영체제

운영체제는 하드웨어와 사용자/프로그램 사이에서 자원을 관리하는 시스템 소프트웨어다.

| 기능 | 설명 |
|---|---|
| 프로세스 관리 | 실행 중인 프로그램 관리 |
| 메모리 관리 | 메모리 할당, 회수, 보호 |
| 파일 시스템 관리 | 파일 저장, 삭제, 접근 제어 |
| 입출력 관리 | 키보드, 디스크, 네트워크 장치 제어 |
| 보안 관리 | 사용자 권한, 접근 제어 |

### 1.5 프로세스와 스레드

| 구분 | 프로세스 | 스레드 |
|---|---|---|
| 의미 | 실행 중인 프로그램 | 프로세스 안의 실행 흐름 |
| 메모리 | 독립적인 메모리 공간 | 같은 프로세스의 메모리 공유 |
| 생성 비용 | 큼 | 작음 |
| 안정성 | 하나가 죽어도 다른 프로세스 영향 적음 | 하나의 오류가 전체 프로세스에 영향 가능 |

#### 프로세스 상태

| 상태 | 설명 |
|---|---|
| New | 생성 중 |
| Ready | CPU 할당 대기 |
| Running | CPU에서 실행 중 |
| Waiting/Blocked | 입출력 등 이벤트 대기 |
| Terminated | 실행 종료 |

### 1.6 스케줄링

스케줄링은 여러 프로세스 중 어떤 프로세스에 CPU를 줄지 결정하는 방식이다.

| 알고리즘 | 설명 | 특징 |
|---|---|---|
| FCFS | 먼저 온 순서대로 실행 | 단순하지만 긴 작업이 앞에 있으면 대기 증가 |
| SJF | 실행 시간이 짧은 작업 우선 | 평균 대기 시간 감소 |
| Round Robin | 정해진 시간만큼 번갈아 실행 | 시분할 시스템에 적합 |
| Priority | 우선순위 높은 작업 먼저 실행 | 낮은 우선순위 작업 기아 가능 |

### 1.7 인터럽트

인터럽트는 CPU가 현재 작업을 멈추고 긴급한 작업을 처리하도록 알리는 신호다.

| 종류 | 설명 |
|---|---|
| 하드웨어 인터럽트 | 입출력 장치, 타이머 등 외부 장치가 발생 |
| 소프트웨어 인터럽트 | 시스템 콜, 예외 상황 등 프로그램 실행 중 발생 |

#### 인터럽트 처리 흐름

1. 인터럽트 발생
2. 현재 실행 상태 저장
3. 인터럽트 서비스 루틴 ISR 실행
4. 저장한 상태 복구
5. 원래 프로그램 실행 재개

### 1.8 시스템 콜

시스템 콜은 사용자 프로그램이 운영체제 기능을 요청하는 인터페이스다.

예시:

- 파일 열기/읽기/쓰기
- 프로세스 생성
- 메모리 할당
- 네트워크 통신

### 1.9 가상 메모리

가상 메모리는 실제 RAM보다 큰 메모리를 사용하는 것처럼 보이게 하는 기법이다.

| 개념 | 설명 |
|---|---|
| 페이지 | 가상 메모리를 일정 크기로 나눈 단위 |
| 프레임 | 물리 메모리를 일정 크기로 나눈 단위 |
| 페이지 테이블 | 가상 주소와 물리 주소의 매핑 정보 |
| 페이지 폴트 | 필요한 페이지가 메모리에 없을 때 발생 |
| 스와핑 | 메모리와 디스크 사이에서 데이터를 교체 |

### 1.10 파일 시스템

파일 시스템은 데이터를 파일과 디렉터리 형태로 저장하고 관리한다.

핵심 기능:

- 파일 생성, 삭제, 읽기, 쓰기
- 디렉터리 구조 관리
- 접근 권한 관리
- 저장 공간 할당

### 1.11 네트워크 기초

| 개념 | 설명 |
|---|---|
| IP | 네트워크에서 장치를 식별하는 주소 |
| TCP | 신뢰성 있는 연결 지향 전송 |
| UDP | 빠르지만 신뢰성을 보장하지 않는 전송 |
| DNS | 도메인 이름을 IP 주소로 변환 |
| HTTP/HTTPS | 웹 통신 프로토콜 |

#### TCP와 UDP 비교

| 구분 | TCP | UDP |
|---|---|---|
| 연결 | 연결 지향 | 비연결 |
| 신뢰성 | 높음 | 낮음 |
| 속도 | 상대적으로 느림 | 빠름 |
| 사용 예 | 웹, 이메일, 파일 전송 | 스트리밍, 게임, VoIP |

---

## 2. 임베기 핵심 정리

> 임베기 = 임베디드 시스템 기초/임베디드 관련 과목으로 보고 정리함.

### 2.1 임베디드 시스템

임베디드 시스템은 특정 기능을 수행하기 위해 기계나 장치 안에 내장된 컴퓨터 시스템이다.

예시:

- 자동차 ECU
- 스마트워치
- 세탁기 제어기
- 의료기기
- IoT 센서
- 로봇 제어 장치

### 2.2 임베디드 시스템 특징

| 특징 | 설명 |
|---|---|
| 전용성 | 특정 기능 수행 목적 |
| 실시간성 | 정해진 시간 안에 반응 필요 |
| 제한된 자원 | CPU, 메모리, 전력 제한 |
| 높은 신뢰성 | 오작동 시 위험할 수 있음 |
| 저전력 | 배터리 기반 장치가 많음 |

### 2.3 마이크로프로세서와 마이크로컨트롤러

| 구분 | 마이크로프로세서 MPU | 마이크로컨트롤러 MCU |
|---|---|---|
| 구성 | CPU 중심 | CPU, 메모리, 주변장치 내장 |
| 외부 부품 | 많이 필요 | 상대적으로 적음 |
| 성능 | 높음 | 낮거나 중간 |
| 전력 | 상대적으로 큼 | 낮음 |
| 사용 예 | PC, 고성능 보드 | 센서, 가전, 제어기 |

### 2.4 임베디드 하드웨어 구성

| 구성 요소 | 설명 |
|---|---|
| CPU/Core | 명령어 실행 |
| Flash | 프로그램 저장, 전원 꺼져도 유지 |
| SRAM | 실행 중 데이터 저장, 전원 꺼지면 사라짐 |
| GPIO | 디지털 입출력 핀 |
| Timer | 시간 측정, 주기적 이벤트 생성 |
| ADC | 아날로그 신호를 디지털 값으로 변환 |
| DAC | 디지털 값을 아날로그 신호로 변환 |
| UART | 직렬 통신 |
| SPI | 고속 동기식 직렬 통신 |
| I2C | 두 선을 사용하는 동기식 직렬 통신 |

### 2.5 GPIO

GPIO는 범용 디지털 입출력 핀이다.

| 모드 | 설명 |
|---|---|
| Input | 외부 신호를 읽음 |
| Output | 신호를 출력 |
| Pull-up | 입력이 떠 있지 않도록 기본값을 High로 유지 |
| Pull-down | 입력이 떠 있지 않도록 기본값을 Low로 유지 |

예시:

- LED 켜기/끄기
- 버튼 입력 읽기
- 릴레이 제어
- 센서 신호 입력

### 2.6 타이머

타이머는 일정 시간 간격을 만들거나 시간을 측정하는 장치다.

활용:

- 주기적 인터럽트 발생
- PWM 신호 생성
- 시간 지연
- 이벤트 간 시간 측정

### 2.7 PWM

PWM은 디지털 신호의 듀티비를 조절해 아날로그처럼 제어하는 방식이다.

| 개념 | 설명 |
|---|---|
| 주기 | 신호가 반복되는 시간 |
| 듀티비 | 한 주기에서 High 상태인 비율 |
| 듀티비 증가 | 평균 출력 증가 |
| 듀티비 감소 | 평균 출력 감소 |

사용 예:

- LED 밝기 조절
- 모터 속도 제어
- 부저 음 조절

### 2.8 ADC와 DAC

| 구분 | 설명 | 예시 |
|---|---|---|
| ADC | 아날로그 값을 디지털 값으로 변환 | 온도 센서 읽기 |
| DAC | 디지털 값을 아날로그 값으로 변환 | 오디오 출력 |

ADC 해상도 예시:

- 10비트 ADC: 0~1023
- 12비트 ADC: 0~4095

### 2.9 통신 방식

#### UART

- 비동기 직렬 통신
- TX, RX 선 사용
- 설정값: Baud rate, 데이터 비트, 패리티, 스톱 비트
- 예시: PC와 MCU 간 시리얼 통신

#### SPI

- 동기식 직렬 통신
- 비교적 빠름
- 주로 4개 선 사용: MOSI, MISO, SCLK, CS
- 마스터-슬레이브 구조
- 예시: 디스플레이, Flash 메모리, 센서

#### I2C

- 동기식 직렬 통신
- 2개 선 사용: SDA, SCL
- 여러 장치를 같은 버스에 연결 가능
- 각 장치는 주소를 가짐
- 예시: 온도 센서, RTC, EEPROM

#### 통신 비교

| 구분 | UART | SPI | I2C |
|---|---|---|---|
| 동기/비동기 | 비동기 | 동기 | 동기 |
| 선 개수 | 보통 2개 | 보통 4개 이상 | 2개 |
| 속도 | 중간 | 빠름 | 중간 |
| 다중 장치 | 제한적 | CS 핀 필요 | 주소 기반으로 쉬움 |

### 2.10 인터럽트와 폴링

| 구분 | 폴링 | 인터럽트 |
|---|---|---|
| 방식 | CPU가 계속 상태 확인 | 이벤트 발생 시 CPU에 알림 |
| CPU 사용 | 많음 | 적음 |
| 구현 | 단순 | 상대적으로 복잡 |
| 반응성 | 확인 주기에 따라 달라짐 | 빠름 |

### 2.11 실시간 시스템

실시간 시스템은 결과의 정확성뿐 아니라 **시간 안에 처리되는 것**이 중요하다.

| 종류 | 설명 | 예시 |
|---|---|---|
| Hard Real-Time | 마감 시간 초과가 치명적 | 자동차 에어백, 항공 제어 |
| Soft Real-Time | 지연이 성능 저하로 이어짐 | 동영상 스트리밍 |
| Firm Real-Time | 늦은 결과는 가치가 거의 없음 | 실시간 주문 처리 |

### 2.12 RTOS

RTOS는 실시간 처리를 지원하는 운영체제다.

주요 기능:

- 태스크 관리
- 우선순위 기반 스케줄링
- 세마포어, 뮤텍스, 큐
- 타이머
- 인터럽트 처리 지원

#### 태스크 상태

| 상태 | 설명 |
|---|---|
| Ready | 실행 대기 |
| Running | 실행 중 |
| Blocked | 이벤트나 자원 대기 |
| Suspended | 강제로 중단된 상태 |

### 2.13 동기화

| 개념 | 설명 |
|---|---|
| 임계구역 | 동시에 접근하면 문제가 생기는 코드 영역 |
| 뮤텍스 | 하나의 태스크만 자원 접근 가능 |
| 세마포어 | 자원 개수나 이벤트를 관리 |
| 데드락 | 서로 자원을 기다리며 멈춘 상태 |
| 우선순위 역전 | 낮은 우선순위 태스크가 높은 우선순위 태스크를 막는 현상 |

### 2.14 Watchdog Timer

Watchdog Timer는 시스템이 멈췄을 때 자동으로 재시작시키는 안전장치다.

동작 방식:

1. 프로그램이 주기적으로 watchdog을 갱신한다.
2. 문제가 생겨 갱신하지 못하면 타이머가 만료된다.
3. 시스템이 리셋된다.

### 2.15 부트 과정

일반적인 임베디드 시스템 부팅 흐름:

1. 전원 인가
2. Reset vector 실행
3. 부트로더 실행
4. 하드웨어 초기화
5. 펌웨어 또는 OS 로딩
6. main 함수 또는 태스크 실행

### 2.16 크로스 컴파일

크로스 컴파일은 개발 PC와 실행 대상 시스템의 CPU 구조가 다를 때 사용하는 빌드 방식이다.

예시:

- PC: x86
- 타깃 보드: ARM
- PC에서 ARM용 실행 파일을 빌드

### 2.17 디버깅

| 방법 | 설명 |
|---|---|
| printf 디버깅 | UART 등으로 로그 출력 |
| JTAG/SWD | 하드웨어 디버거로 레지스터, 메모리 확인 |
| 오실로스코프 | 전기 신호 파형 확인 |
| 로직 분석기 | 디지털 통신 신호 분석 |

---

## 3. 자주 나오는 비교 정리

### 3.1 RAM과 ROM

| 구분 | RAM | ROM/Flash |
|---|---|---|
| 휘발성 | 휘발성 | 비휘발성 |
| 용도 | 실행 중 데이터 저장 | 프로그램 저장 |
| 속도 | 빠름 | 상대적으로 느림 |
| 전원 차단 | 데이터 사라짐 | 데이터 유지 |

### 3.2 캐시와 버퍼

| 구분 | 캐시 | 버퍼 |
|---|---|---|
| 목적 | 접근 속도 향상 | 속도 차이 흡수, 임시 저장 |
| 기준 | 자주 사용할 데이터 | 전송 중인 데이터 |
| 예시 | CPU 캐시 | 키보드 버퍼, 네트워크 버퍼 |

### 3.3 인터럽트와 예외

| 구분 | 인터럽트 | 예외 |
|---|---|---|
| 원인 | 외부 장치 또는 소프트웨어 요청 | 프로그램 실행 중 오류/특수 상황 |
| 예시 | 타이머, 키보드, 입출력 완료 | 0으로 나누기, 페이지 폴트 |

### 3.4 펌웨어와 소프트웨어

| 구분 | 펌웨어 | 소프트웨어 |
|---|---|---|
| 위치 | 장치 내부 Flash/ROM | 저장장치 또는 메모리 |
| 목적 | 하드웨어 제어 | 다양한 응용 기능 |
| 변경 | 상대적으로 드묾 | 자주 변경 가능 |

---

## 4. 시험 직전 암기 포인트

### 컴시기

- CPU는 **명령어 인출, 해석, 실행**을 반복한다.
- 레지스터는 CPU 내부의 가장 빠른 저장 공간이다.
- 캐시는 **지역성**을 이용해 속도를 높인다.
- 운영체제는 자원 관리자다.
- 프로세스는 독립 실행 단위, 스레드는 프로세스 내부 실행 흐름이다.
- 인터럽트는 CPU에게 긴급 처리를 요청하는 신호다.
- 시스템 콜은 사용자 프로그램이 커널 기능을 요청하는 통로다.
- 가상 메모리는 페이지와 페이지 테이블을 통해 구현된다.

### 임베기

- 임베디드 시스템은 특정 목적을 위한 내장형 컴퓨터다.
- MCU는 CPU, 메모리, 주변장치를 한 칩에 포함한다.
- GPIO는 디지털 입력/출력 핀이다.
- PWM은 듀티비로 평균 출력을 조절한다.
- ADC는 아날로그를 디지털로, DAC는 디지털을 아날로그로 바꾼다.
- UART는 비동기, SPI와 I2C는 동기식 통신이다.
- RTOS는 실시간 태스크 스케줄링을 지원한다.
- Watchdog은 시스템이 멈췄을 때 리셋하는 안전장치다.

---

## 5. 단답형 예상 문제

### Q1. 운영체제의 역할은?

하드웨어 자원을 관리하고 사용자와 응용 프로그램이 컴퓨터를 편리하게 사용할 수 있도록 지원한다.

### Q2. 프로세스와 스레드의 차이는?

프로세스는 독립적인 실행 단위이고, 스레드는 프로세스 내부에서 실행되는 흐름이다. 스레드는 같은 프로세스의 메모리를 공유한다.

### Q3. 캐시 메모리를 사용하는 이유는?

CPU와 주기억장치 사이의 속도 차이를 줄이고 자주 사용하는 데이터에 빠르게 접근하기 위해서다.

### Q4. 인터럽트란?

CPU가 현재 작업을 중단하고 특정 사건을 먼저 처리하도록 하는 신호다.

### Q5. 임베디드 시스템의 특징은?

특정 기능 수행, 제한된 자원, 실시간성, 높은 신뢰성, 저전력성이 대표적이다.

### Q6. MCU와 MPU의 차이는?

MCU는 CPU, 메모리, 주변장치가 한 칩에 통합된 제어용 칩이고, MPU는 CPU 중심의 고성능 처리 장치로 외부 메모리와 주변장치가 필요하다.

### Q7. 폴링과 인터럽트의 차이는?

폴링은 CPU가 상태를 반복 확인하는 방식이고, 인터럽트는 이벤트가 발생했을 때 CPU에게 알려주는 방식이다.

### Q8. PWM의 용도는?

듀티비를 조절해 LED 밝기, 모터 속도, 부저 소리 등을 제어한다.

### Q9. UART, SPI, I2C의 차이는?

UART는 비동기 직렬 통신이고, SPI는 빠른 동기식 통신이며, I2C는 두 선으로 여러 장치를 주소 기반으로 연결하는 동기식 통신이다.

### Q10. Watchdog Timer의 목적은?

프로그램이 멈추거나 비정상 동작할 때 시스템을 자동으로 리셋해 복구하기 위해 사용한다.

---

## 6. 헷갈리기 쉬운 포인트

- **프로그램**은 저장장치에 있는 파일이고, **프로세스**는 실행 중인 프로그램이다.
- **동시성**은 여러 작업이 번갈아 실행되는 것이고, **병렬성**은 실제로 동시에 실행되는 것이다.
- **캐시**는 속도 향상 목적, **버퍼**는 전송 속도 차이를 완화하는 목적이다.
- **인터럽트**는 외부 사건 처리에 많이 쓰이고, **예외**는 실행 중 오류나 특수 상황과 관련된다.
- **Hard real-time**은 마감 시간 초과가 치명적이다.
- **Flash**는 전원이 꺼져도 내용이 유지되고, **SRAM**은 전원이 꺼지면 내용이 사라진다.

---

## 7. 빠른 복습 체크리스트

- [ ] CPU 구성 요소를 설명할 수 있다.
- [ ] Fetch-Decode-Execute 과정을 설명할 수 있다.
- [ ] 캐시의 지역성을 설명할 수 있다.
- [ ] 프로세스와 스레드를 비교할 수 있다.
- [ ] 인터럽트 처리 과정을 설명할 수 있다.
- [ ] 가상 메모리와 페이지 폴트를 설명할 수 있다.
- [ ] MCU와 MPU를 비교할 수 있다.
- [ ] GPIO, Timer, PWM, ADC의 용도를 설명할 수 있다.
- [ ] UART, SPI, I2C를 비교할 수 있다.
- [ ] RTOS와 일반 OS의 차이를 설명할 수 있다.
- [ ] Watchdog Timer의 목적을 설명할 수 있다.

