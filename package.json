{
  "name": "certificate-practice",
  "version": "0.1.0",
  "private": true,
  "scripts": {
    "questions:generate": "node scripts/generate-questions.mjs",
    "predev": "npm run questions:generate",
    "dev": "next dev",
    "prebuild": "npm run questions:generate",
    "build": "next build",
    "start": "next start",
    "lint": "tsc --noEmit",
    "test": "node --disable-warning=MODULE_TYPELESS_PACKAGE_JSON --experimental-strip-types --test tests/*.test.mjs"
  },
  "dependencies": {
    "@supabase/supabase-js": "^2.110.8",
    "next": "latest",
    "react": "latest",
    "react-dom": "latest"
  },
  "devDependencies": {
    "@types/node": "latest",
    "@types/react": "latest",
    "@types/react-dom": "latest",
    "pdfjs-dist": "^6.1.200",
    "typescript": "5.9.3"
  },
  "overrides": {
    "postcss": "8.5.21",
    "sharp": "0.35.3"
  }
}
